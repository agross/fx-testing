@page
@model Web.Pages.Prüflinge.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Prüfling[0].Typ)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prüfling[0].Bezeichnung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prüfling[0].Straße)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prüfling[0].Haus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prüfling[0].Ort)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Prüfling) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Typ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bezeichnung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Straße)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Haus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ort)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
